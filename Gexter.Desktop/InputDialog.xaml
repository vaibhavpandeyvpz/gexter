<Window x:Class="Gexter.Desktop.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        Title="Input"
        Width="350"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="None"
        Background="{StaticResource SecondaryBrush}">

    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock x:Name="TitleText" Text="Input" 
                               Foreground="{StaticResource TextPrimaryBrush}"
                               VerticalAlignment="Center" Margin="10,0,0,0" FontSize="12"/>
                    <Button Style="{StaticResource TitleBarCloseButton}" 
                            HorizontalAlignment="Right"
                            Click="CloseButton_Click">
                        <fa:ImageAwesome Icon="Times" Foreground="{StaticResource TextPrimaryBrush}" Width="10" Height="10"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="16,12">
                <TextBlock x:Name="PromptText" Text="Enter value:"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           Margin="0,0,0,8"/>
                <TextBox x:Name="InputTextBox" 
                         Style="{StaticResource FlatTextBox}"
                         Padding="8,6"/>
            </StackPanel>

            <!-- Buttons -->
            <Border Grid.Row="2" Background="{StaticResource PrimaryBrush}" Padding="16,10">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="OK" 
                            Style="{StaticResource AccentButton}"
                            Width="80" Padding="0,6"
                            Click="OkButton_Click"/>
                    <Button Content="Cancel" 
                            Style="{StaticResource FlatButton}"
                            Width="80" Padding="0,6"
                            Margin="8,0,0,0"
                            Click="CancelButton_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>

